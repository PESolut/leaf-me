import React, { useEffect } from "react";

const SignInForm2 = ({ onSubmit, isNewUser, userEmail, stage, isNew }) => {
    useEffect(() => {
        console.log('email',userEmail, 'stage number:', stage, 'isNew:', isNew)
    }, [])

    return (
        <>
        
        </>
    )
}

export default SignInForm2
